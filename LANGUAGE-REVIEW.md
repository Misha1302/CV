# Language review — v25

## Priority surfaces

- `ru/en-compiler.html`: Wist2 first; PS-form Analyzer and the x86-64 lab provide program-analysis and backend depth; `AdvancedAlgorithms` remains a separate compact C++ project.
- `ru/en.html`: full technical portfolio, not a catch-all application CV.

## Corrections

- Russian compiler headings are now `Архитектура и IR`, `Анализ и оптимизации`, `Проверка корректности`, `Языки и инструменты`.
- The rejected headings `Compiler architecture`, `Analysis & optimizations` and `Toolchain и C++-основа` are absent from Russian HTML and PDF.
- Skills are phrased as complete technical statements rather than comma-separated keyword dumps.
- `AdvancedAlgorithms` is no longer advertised inside the compiler toolchain row.
- Full RU/EN distinguish primary and complementary expertise instead of presenting every technology at the same level.
- Modified English pages contain no unintended Cyrillic text; the Russian-language switch accessibility label is the only expected Cyrillic string.
- IR, SSA, backend, runtime, SCCP and DCE are retained where they are standard role vocabulary.
- No `expert`, `senior`, `production-ready`, formal-verification, HFT or unsupported performance claims were introduced.

Verdict: PASS.
