name: Rebuild CV site (paused during source migration)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Source migration in progress; final workflow will replace this file."
